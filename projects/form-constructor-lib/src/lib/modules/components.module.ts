import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Components module that contains all UI components
 * for the form constructor library.
 *
 * Note: This module is used to provide common dependencies for standalone components.
 * The actual components are imported directly where needed.
 */
@NgModule({
  imports: [
    CommonModule
  ]
})
export class ComponentsModule { }