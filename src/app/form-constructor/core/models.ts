/**
 * Core Models for Form Constructor
 */

// Import unified models from the core models
import type {
  ComponentDefinition,
  BlockDefinition
} from '../../core/models/component.model';

// Re-export for backward compatibility
export type { ComponentDefinition, BlockDefinition };